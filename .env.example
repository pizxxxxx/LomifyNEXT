# Optional shared credentials for release builds. Leave empty during ordinary development.
# When they are present, Settings shows a single provider sign-in button instead of asking
# every user to paste developer credentials.
VITE_LASTFM_API_KEY=
VITE_LASTFM_SHARED_SECRET=
